<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.HorizonteNews"
        android:usesCleartextTraffic="true">

        <activity
            android:name="com.horizontenews.app.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.horizontenews.app.DetailActivity"
            android:exported="false" />

        <activity
            android:name="com.horizontenews.app.SearchActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- NOVA ATIVIDADE PARA O MENU DE CONFIGURAÇÕES -->
        <activity
            android:name="com.horizontenews.app.ConfiguracoesActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- SERVIÇO DE NOTIFICAÇÕES DO FIREBASE -->
        <service
            android:name="com.horizontenews.app.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>