package com.horizontenews.app

import android.content.Intent
import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity
import androidx.recyclerview.widget.LinearLayoutManager
import androidx.recyclerview.widget.RecyclerView

class ConfiguracoesActivity : AppCompatActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_configuracoes)

        val toolbar = findViewById<androidx.appcompat.widget.Toolbar>(R.id.toolbar_config)
        setSupportActionBar(toolbar)
        supportActionBar?.setDisplayHomeAsUpEnabled(true)

        toolbar.setNavigationOnClickListener { finish() }

        val recyclerView = findViewById<RecyclerView>(R.id.recycler_configuracoes)
        recyclerView.layoutManager = LinearLayoutManager(this)

        val items = listOf(
            ConfigItem(
                title = "Tema",
                iconRes = R.drawable.ic_settings_gear,
                action = { /* TODO: Trocar tema */ }
            ),
            ConfigItem(
                title = "Compartilhar App",
                iconRes = R.drawable.ic_share_black_custom,
                action = {
                    val shareIntent = Intent(Intent.ACTION_SEND).apply {
                        type = "text/plain"
                        putExtra(Intent.EXTRA_TEXT, "Baixe o Horizonte News!")
                    }
                    startActivity(Intent.createChooser(shareIntent, "Compartilhar"))
                }
            ),
            ConfigItem(
                title = "Sobre o App",
                iconRes = R.drawable.ic_info, // se não tiver, pode usar outro ícone
                action = { /* TODO: Tela sobre */ }
            )
        )

        recyclerView.adapter = ConfiguracoesAdapter(items)
    }
}